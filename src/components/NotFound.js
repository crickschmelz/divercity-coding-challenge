import React from "react";

// used as a placeholder for any URL not specified in Router component
const NotFound = () => {
  return (
    <div>
      <h2>NOT FOUND</h2>
    </div>
  );
};

export default NotFound;
